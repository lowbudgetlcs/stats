{
  "name": "stats",
  "version": "1.0.0",
  "description": "",
  "main": "captureMatchDTO.js",
  "scripts": {
    "start": "npx functions-framework --target=captureMatchDTO [--signature-type=http]",
    "test": "echo \"Error: no test specified\" && exit 1",
    "pretty": "npx eslint *.js --fix"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.1.4"
  }
}
